describe('requestJSONs', () => {
  beforeEach(() => {});

  describe('validation', () => {
    it('validates arguments are the correct shape', () => {});
    it('validates arguments are URLs', () => {});
  });

  it('returns results in the correct shape', () => {});
});
